{
  "name": "bloxorz-html5",
  "private": true,
  "version": "2.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 4398",
    "build": "node -e \"import { cpSync, rmSync } from 'node:fs'; rmSync('dist',{recursive:true,force:true}); cpSync('src','dist',{recursive:true});\"",
    "preview": "vite preview --host --port 4398",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.3.1",
    "vite": "^6.0.7"
  }
}
